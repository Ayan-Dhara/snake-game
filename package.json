{
  "name": "snake-game",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "build": "sass public:public --no-source-map",
    "start": "node main.js",
    "dev": "sass --watch public:public --no-source-map & node main.js",
    "live": "sass --watch public:public --no-source-map & cd public && live-server --no-browser"
  },
  "keywords": [
    "snake"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "sass": "^1.35.1"
  }
}
